<header>
  <nav class="navbar navbar-inverse bg-primary navbar-fixed-top navbar-toggleable-md bg-faded">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <%= link_to 'CableChat', root_path, id: 'logo' %>

    <div>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to '채팅 리스트', root_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to '새 채팅방 만들기', new_chat_room_path, class: 'nav-link' %>
        </li>

        <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "로그 아웃", destroy_user_session_path, method: :delete , class: 'nav-link' %>
            </li>
        <% else %>
            <li class="nav-item">
              <%= link_to '로그인', new_user_session_path, class: 'nav-link'  %>
            </li>
        <% end %>
      </ul>
    </div>
  </nav>
</header>